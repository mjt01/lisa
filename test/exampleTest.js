curl(['textfield'], function(TextField) {
	describe('TextField Widget', function(){
		it('should fire change event when value changes', function() {
	 		// var txt = new TextField(), fired = false;
			// txt.on("change", function() { fired = true; });

			// expect(fired).toEqual(true);
		});
    });
});